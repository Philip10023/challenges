var Rocket = class Rocket {
  constructor(){
    this.fuel = 0;
    this.addFuel = function(fuel) {
      let addFuel = 50;
    this.fire = function(){
      if(this.fuel === 0)
      return false && `Not enough fuel!`;
      else if(this.fuel > 0);
    let fire = this.fuel - 1;
    return true && `Rockets fired! :${Rocket.fuel} fuel left`;
    };
    return Rocket.fuel;
    };


  }

};
module.exports = Rocket;
