let mainDiv = document.getElementById('result');
let question = prompt('at what temperature on the celcius scale does water freeze?', " ");

if (question == 0)

  mainDiv.innerHTML = "smart cookie!";
 else
  mainDiv.innerHTML = 'wow what a dumbass';
